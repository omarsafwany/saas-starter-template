name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder success step
        run: echo "CI placeholder - PERPRO-18 will replace this with the real pipeline"
